# Core web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # required for FastAPI file uploads

# AI / LLM
google-genai>=1.0.0          # Gemini API (new SDK — replaces deprecated google-generativeai)

# Settings / config
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Database (PostgreSQL + async driver)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# pgvector — vector similarity search inside PostgreSQL
pgvector>=0.3.0

# Optional semantic retrieval runtime (the application degrades to lexical
# retrieval when these or their model assets are unavailable)
qdrant-client>=1.7.0
sentence-transformers>=2.7.0

# Document processing
PyMuPDF>=1.24.0              # Fast PDF text extraction with excellent Unicode/Hindi support
python-docx>=1.1.0           # DOCX extraction

# HTTP client — for source adapters calling external APIs/portals
httpx>=0.27.0
beautifulsoup4>=4.12.0

# Utilities
python-dotenv>=1.0.0

# Test runner
pytest>=8.0.0
pytest-asyncio>=0.23.0
reportlab==5.0.1
